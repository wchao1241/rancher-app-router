apiVersion: v1
kind: Service
metadata:
  name: nginx-service-without-hostname
  labels:
    k8s-app: nginx-service-without-hostname
spec:
  ports:
  - port: 90
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    k8s-app: nginx-service-without-hostname
---
apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-service-without-hostname
spec:
  replicas: 1
  selector:
    k8s-app: nginx-service-without-hostname
  template:
    metadata:
      labels:
        k8s-app: nginx-service-without-hostname
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: nginx-service-without-hostname
        image: nginx:latest
        ports:
        - containerPort: 80
